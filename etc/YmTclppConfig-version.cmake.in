set (PACKAGE_VERSION "@YMTCLPP_VERSION@")
if (NOT "${PACKAGE_FIND_VERSION}" VERSION_GREATER "@YMTCLPP_VERSION@")
  set (PACKAGE_VERSION_COMPATIBLE 1)
  if ("${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@YMTCLPP_VERSION@")
    set (PACKAGE_VERSION_EXACT 1)
  endif ()
endif ()